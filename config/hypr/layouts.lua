hl.config({
    general = {
        layout = scrolling
    },

    dwindle = {
        preserve_split = true,
        smart_split = false,
        smart_resizing = false,
    	precise_mouse_move = true,
    },

    scrolling = {
        column_width = 0.8,
        follow_min_visible = 0.1,
        -- focus_fit_method = 0
    }
    

})
